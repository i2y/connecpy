[project]
name = "connect-python-conformance"
version = "0.1.0"

dependencies = [
    "brotli",
    "connect-python",
    "httpx[http2]",
    "hypercorn",
    "pytest",
    "pytest-asyncio",
    "pytest-cov",
    "zstandard",
]

[tool.ruff]
extend = "../pyproject.toml"
