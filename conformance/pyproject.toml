[project]
name = "connecpy-conformance"
version = "0.1.0"

dependencies = [
    "brotli",
    "connecpy",
    "httpx[http2]",
    "pytest",
    "pytest-asyncio",
    "zstandard",
]

[tool.ruff]
extend = "../pyproject.toml"
